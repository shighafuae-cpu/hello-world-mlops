# hello-world-mlops
the first CI for push the model automatically (install depencies, train and push the model)

## Cmd
git add .
git commit -m "first commit"
git branch -M main
git push -u origin main